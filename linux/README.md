## Linux

Options for both system wide and user wide installations. User wide method is recommended, since it doesn't require sudo, and it's fully persistent. The system wide files can get overwritten with some system updates meaning you have to reinstall the layout after breaking changes. Breaking updates are rare but still annoying.
